#machine:
  #services:
    #- docker
  #ruby:
    #version: 2.3.1

dependencies:
  pre:
    - |
      if [[ $CIRCLE_NODE_INDEX = 0 ]]; then
        gem install bundler && bundle install && touch ruby_deps_installed ;
      else
        ssh node0 "ls $HOME/"
      fi
    #- sleep 550


test:
  override:
    #- sleep 550
    #- pkg-versions.sh
    - exit 0

#deployment:
  #test:
    #tag: /\d+\.\d+\.\d+/
    #commands:
      #- exit 0

  #test-with-dashes:
    #tag: /(?i).*-rc\d+/
    #commands:
      #- echo "test with dashes"


