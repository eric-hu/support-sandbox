#machine:
  #ruby:
    #version: 2.3.1

#dependencies:
  #post:
    #- export FOO=bar

test:
  override:
    - exit 0
    - echo $CIRCLE_TAG

deployment:
  test:
    tag: /\d+\.\d+\.\d+/
    commands:
      - exit 0

  test-with-dashes:
    tag: /.*-rc\d+/
    commands:
      - echo "test with dashes"
