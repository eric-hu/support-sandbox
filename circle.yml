machine:
  services:
    - redis

test:
  override:
    - "exit 0"
    - "ls -la /var/run/redis/"
    - "cat /var/run/redis/redis-server.pid"
    - "sudo tail --lines=20 /var/log/redis/redis-server.log"

