#dependencies:
  #post:
    #- export FOO=bar

test:
  override:
    - exit 0


deployment:
  #dev-tag:
    #*misc-dev
  dev-tag:
    branch: dev
    owner: foo
    commands:
      - echo "dev"

  dev-branch:
    tag: /dev-.*/
    owner: foo
    commands:
      - echo "dev"

  qa:
    tag: /qa-.*/
    owner: foo
    commands:
      - echo "qa"
  prod:
    tag: /prod-.*/
    owner: foo
    commands:
      - echo "prod"
