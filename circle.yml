machine:
  ruby:
    version: 2.3.1

#dependencies:
  #post:
    #- export FOO=bar

test:
  pre:
    - echo "node: $CIRCLE_NODE_INDEX" > $CIRCLE_ARTIFACTS/node-pre.txt
  override:
    - exit 0
  pre:
    - echo "node: $CIRCLE_NODE_INDEX" > $CIRCLE_ARTIFACTS/node-post.txt

#deployment:
  #test:
    #tag: /\d+\.\d+\.\d+/
    #commands:
      #- exit 0

  #test-with-dashes:
    #tag: /(?i).*-rc\d+/
    #commands:
      #- echo "test with dashes"

