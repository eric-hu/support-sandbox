#machine:
  #services:
    #- redis
  #ruby:
    #version: 2.2.2

#dependencies:
  #pre:
    ##- sleep 550

checkout:
  post:
    - echo "test"

#machine:
  #post:
    #- pyenv global 2.7 3.4

test:
  pre:
    - python --version
    - docker login -u $DOCKER_USER -p $DOCKER_PASSWORD && docker info | grep -i username
    - docker info | grep -i username
  #override:
    #- which google-chrome && google-chrome --version || echo "no google-chrome found in PATH"
    #- which chromedriver && chromedriver --version || echo "no chrome found in PATH"
