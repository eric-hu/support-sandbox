machine:
  pre:
    - sudo echo "nameserver 10.2.216.2" > /etc/resolv.conf
    - sudo cat /etc/resolv.conf

  #ruby:
    #version: 2.3.1

#dependencies:
  #post:
    #- export FOO=bar

test:
  override:
    - sudo cat /etc/resolv.conf
    - exit 0

#deployment:
  #test:
    #tag: /\d+\.\d+\.\d+/
    #commands:
      #- exit 0

  #test-with-dashes:
    #tag: /(?i).*-rc\d+/
    #commands:
      #- echo "test with dashes"

