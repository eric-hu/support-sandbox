#machine:
  #ruby:
    #version: 2.3.1

dependencies:
  pre:
    - sleep 550


test:
  override:
    - sleep 550
    - exit 0

#deployment:
  #test:
    #tag: /\d+\.\d+\.\d+/
    #commands:
      #- exit 0

  #test-with-dashes:
    #tag: /(?i).*-rc\d+/
    #commands:
      #- echo "test with dashes"


