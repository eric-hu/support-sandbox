machine:
  ruby:
    version: 2.3.1

dependencies:
  pre:
    - echo y | android update sdk --no-ui --all --filter "build-tools-24.0.0"

test:
  override:
    - exit 0

#deployment:
  #test:
    #tag: /\d+\.\d+\.\d+/
    #commands:
      #- exit 0

  #test-with-dashes:
    #tag: /(?i).*-rc\d+/
    #commands:
      #- echo "test with dashes"

