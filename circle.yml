#machine:
  #services:
    #- docker
  #ruby:
    #version: 2.3.1

dependencies:
  pre:
    - |
      if [[ $CIRCLE_NODE_INDEX = 0 ]]; then
        gem install bundler && bundle install ;
      fi
    - |
      # Don't need parallel true because parallel is implied for all steps in
      # dependency phase
      if [[ $CIRCLE_NODE_INDEX != 0 ]]; then
        ssh node0 "ls -la $HOME/"
        ssh node0 "ls -la $HOME/.bundle"
        ssh node0 "ls -la $HOME/.bundle/cache"
        ssh node0 "ls -la $HOME/support-sandbox"
      fi

    #- sleep 550


test:
  override:
    #- sleep 550
    #- pkg-versions.sh
    - exit 0

#deployment:
  #test:
    #tag: /\d+\.\d+\.\d+/
    #commands:
      #- exit 0

  #test-with-dashes:
    #tag: /(?i).*-rc\d+/
    #commands:
      #- echo "test with dashes"


