machine:
  #services:
    #- docker
  ruby:
    version: 2.2.4

dependencies:
  pre:
    - gem install bundler --version '1.12.5'
    - echo "source 'www.rubygems.org'" > Gemfile
    - bundle install
    #- ls ~/support-sandbox
    #- sleep 550


test:
  override:
    #- sleep 550
    - bundle exec bundle exec ls
    - exit 0
