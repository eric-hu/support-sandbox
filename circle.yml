#machine:
  #services:
    #- redis
  #ruby:
    #version: 2.2.2

#dependencies:
  #pre:
    ##- sleep 550

machine:
  post:
    - pyenv global 2.7.12 3.4.4

test:
  pre:
    - python --version
  #override:
    #- which google-chrome && google-chrome --version || echo "no google-chrome found in PATH"
    #- which chromedriver && chromedriver --version || echo "no chrome found in PATH"
