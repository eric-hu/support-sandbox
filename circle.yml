#machine:
  #ruby:
    #version: 2.3.1

#dependencies:
  #post:
    #- export FOO=bar

test:
  override:
    - exit 0
    - echo $CIRCLE_TAG

deployment:
  test:
    tag: test
    owner: erichu
    commands:
      - exit 0

  circle-yml:
    branch: /.*yml.*/
    commands:
      - echo "circle-yml"
      - exit 0
