machine:
  services:
    - redis
  ruby:
    version: 2.2.2

#dependencies:
  #pre:
    ##- sleep 550


test:
  override:
    - git status
    - exit 0

deployment:
  master:
    - echo "foo"

deployment:
  circle-yml-only:
    - echo "foo"
