#machine:
  #ruby:
    #version: 2.3.1

#dependencies:
  #post:
    #- export FOO=bar

test:
  override:
    - exit 0
    - echo $CIRCLE_TAG

deployment:
  test:
    tag: /[0-9]+\.[0-9]+\.[0-9]+/
    commands:
      - exit 0
